# Taste
- Writes requests in Indonesian (Bahasa Indonesia), often in a casual, non-formal register. Confidence: 0.7
- Gives short, high-level instructions (e.g. "make it more interesting with X, Y, Z", "add a redirect link to repo X", "add illustrations to slides 4 and 5") and expects the agent to choose the approach and handle the details. Confidence: 0.7
- Prefers concrete real-world examples over abstract theory in teaching/materials for students — explicitly asks for "contoh implementasi pada dunia nyata". Confidence: 0.6
- Likes technical concepts explained through everyday analogies/metaphors mapped to the topic (e.g. asks to frame web development as a restaurant: frontend = dining room/menu, backend = kitchen, database = store-room, API = waiter). Confidence: 0.55
- Prefers visually rich, motion-heavy frontends over plain static pages; asks for modern animation/3D libraries by name (GSAP, Framer Motion, Three.js). Confidence: 0.6
- Wants visual content alongside text on content/slide pages — explicitly asks for illustrations/images to be added, so text-only sections are treated as incomplete. Confidence: 0.7
- Names Unsplash as the source for real photos to embed in the UI (rather than only vector/SVG illustrations), and will ask to replace existing inline SVG illustrations on slides with Unsplash photos. Confidence: 0.65
- Wants content elements to be interactive — asks for list/point items or cards on slides to be clickable and reveal detail in a modal instead of showing everything at once, and expects new cards to reuse the established card → modal pattern (image + explanation). Confidence: 0.6
- Wants media assets pulled from free, publicly accessible URLs/APIs that need no API key or paid access (e.g. asks for images "dari public free api url"). Confidence: 0.6
- Wants teaching/material content to cover currently trending technologies and frameworks, not just generic concepts (e.g. asks for explanation of "implementasi teknologi / framework trend saat ini"). Confidence: 0.6
- Wants named technologies paired with their recognizable brand logo/icon (e.g. Laravel, MySQL) rather than plain text mentions, and expects an icon for every technology mentioned (e.g. asks for an icon on each roadmap item). Confidence: 0.7
- Wants the produced material itself (slide/deck copy, on-page text) written in Indonesian, not just the chat replies. Confidence: 0.55
- Wants demo widgets to be fully self-contained front-end apps with state persisted in the browser (explicitly asks for data to be kept in localStorage, no backend required), and expects full CRUD interactions — dashboard with add/edit/delete plus a reset — not just a single submit flow. Confidence: 0.55
- Wants each code/feature slide to carry its own "DEMO" button that opens a modal preview rendering the UI exactly as that slide's code would look (e.g. "buatkan demo pada slide step 12, dan 13, ada button DEMO ketika di klik sesuaikan tampilan form sesuai dengan code tersebut") — i.e. a faithful visual mockup of the form/table the snippet produces, not just an abstract simulator. Confidence: 0.6
- Wants teaching slides to include runnable/simulated interactive demo widgets that walk through a real workflow step by step (e.g. a form whose submit triggers validation, a simulated API POST showing endpoint + JSON payload, a visible loading state, a success message, then a reset) rather than only static explanatory text. Confidence: 0.55
- Prefers teaching materials to open with a "project background/motivation" section right after the intro, framing the real-world problem (why the project exists) before diving into technical steps. Confidence: 0.5
- Wants code/command blocks in teaching material to be one-click copyable — a small icon button in the code block's top-right corner that copies the code on click (with visual feedback), instead of leaving students to select the text manually. Confidence: 0.55
- Wants code blocks in teaching material to be copyable at line granularity too — for multi-line command blocks, each line should expose its own copy control so a single command can be copied, in addition to the whole-block copy. Confidence: 0.5
- After a change is demonstrated in one spot, expects it rolled out consistently to every other applicable instance (e.g. asks to apply a new code-block feature to all slides that contain code, not just the one shown; repeatedly asks to extend the same slide treatment to the next numbered slide, e.g. "implemen juga pada slide 9"; expects a new treatment applied for one slide to be extended to the entire deck, e.g. a quiz added for one slide should be rolled out to every numbered Step). Confidence: 0.75
- Wants teaching/project slides to give students a direct path to the full source code — a GitHub repository link (with the clone command embedded) so the code is downloadable, not just shown as snippets. Confidence: 0.5
- Prefers third-party JS visualization libraries loaded from a CDN (explicitly "chartjs atau library lain by cdn") rather than a bundler/npm build step in these static HTML pages. Confidence: 0.75
- Wants technical/structural relationships (e.g. database table relations) shown as rendered illustrated diagrams rather than ASCII-art or plain text representations. Confidence: 0.6
- Wants teaching snippets to be complete, ready-to-paste artifacts covering the full end-to-end flow (e.g. the generate command, the whole migration file with up/down, then the run commands) — not abbreviated fragments the student would have to fill in; asks for the same "generate command + copy full file" treatment step by step across the deck (repeated for migrations, models, and controllers). Confidence: 0.7
- Prefers explicit, verbose code over framework "magic"/shorthand in examples — e.g. asks to avoid `Route::resource` and `--resource` in favor of declaring each route/method explicitly, even at the cost of more lines. Confidence: 0.55
- Wants long code in teaching material split into labeled sections (e.g. per method: index(), store(), destroy(), plus a declaration/import part), each with a short one-line explanation of what that part does — rather than one monolithic code block the student must interpret alone. Confidence: 0.7
- Wants multi-section code collapsed behind a dropdown/accordion (click the labeled header to reveal that section's code) instead of everything expanded at once — e.g. organizing a page's code into numbered per-action sections (create, update, detail, delete, index). Confidence: 0.4 — subsequently asked for these same page sections to be split into one slide per feature instead, so accordion is only a fallback when content cannot be split across slides.
- In teaching snippets, prefers each file/component to be fully self-contained rather than DRY — explicitly asks for create/update forms to be written inline in each page instead of a shared `@include` partial/component, accepting duplicated markup so every file can stand alone and be copied on its own. Confidence: 0.6
- Prefers one focused topic per slide — repeatedly asks to split a slide that bundles multiple artifacts/items into separate slides, one per topic/file (e.g. Step 6's two migrations → one slide per migration; Step 7's two models → "Model Kelas" and "Model Siswa"; controller slides → "bagi menjadi per function sama dengan per slide", one slide per method; Step 10's route slide → one slide for URL list/setup, one per route group, one for route:list). Confidence: 0.75
- Wants self-check quiz material embedded in teaching decks — asks for multiple-choice quiz slides keyed to the topic of a specific earlier slide, each with exactly 4 answer options and an informational explanation of the correct answer. Confidence: 0.6
- Treats each quiz as its own slide (asked for 5 quizzes placed as 5 separate slides right after the topic they cover), matching the one-topic-per-slide preference. Confidence: 0.7
- Wants quizzes as dedicated slides placed immediately after the material slide they test — explicitly asked to convert inline quizzes into a new slide following the source slide ("untuk kuis buat menjadi slide baru ya! setelah materi dari soal tersebut"), reinforcing one-topic-per-slide over embedding the quiz inside the material section. Confidence: 0.65
- Prefers a measured amount of practice content rather than the maximum — when offered 1 vs 5 quizzes per Step slide, chooses 1 quiz per Step/topic so the whole deck gets coverage without exploding the slide count. Confidence: 0.55
- Uses terse follow-up commands ("implement all", "implement it") to mean "roll the just-established pattern out everywhere else it applies"; expects the agent to work out the target and may ask a scope question rather than spelling scope out up front. Confidence: 0.6
- When extending a second file/deck with the same feature, wants the addition to follow that file's own existing convention and structure (e.g. reuse the sibling deck's inline quiz markup with `data-answer`/`data-reason` and keep its hard-coded slide numbering) rather than porting the first file's markup or inserting new numbered slides. Confidence: 0.5 — an explicit later instruction to split those quizzes out into their own slides (each right after its material slide) overrides reuse of the file's inline arrangement, so match existing conventions unless told to restructure.
